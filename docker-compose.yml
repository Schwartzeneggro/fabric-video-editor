version: '3.8'

services:
  fabric-video-editor:
    build: .
    container_name: fabric-video-editor
    restart: unless-stopped
    ports:
      # Map port 3000 (Next.js default)
      - "3000:3000"
