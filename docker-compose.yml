version: '3.8'

services:
  fabric-video-editor:
    # This tells Portainer: "Look for the Dockerfile and build it"
    build: .
    container_name: fabric-video-editor
    restart: unless-stopped
    ports:
      - "5173:5173"
